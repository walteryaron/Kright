# OS
.DS_Store
*.swp
*.log

# Editors
.idea/
*.iml
.vs/
.vscode/

# ── macOS native app (Mac, Swift / Xcode) ──
Mac/build/
DerivedData/
*.xcuserstate
**/xcuserdata/
*.xcscmblueprint

# ── Windows native app (Windows, .NET / WPF) ──
Windows/bin/
Windows/obj/
Windows/publish/
Windows/installer/output/
[Bb]in/
[Oo]bj/

# ── Signing keys / secrets (NEVER commit or push) ──
secrets/
*-PRIVATE.key
*.private.key

# Local-only internal docs (kept on disk, never pushed)
PRD.md

# Windows Explorer thumbnail cache
Thumbs.db
